[project]
name = "blunder-api"
version = "0.1.0"
dependencies = [
    "alembic>=1.19.2",
    "asyncpg>=0.31.0",
    "blunder-shared",
    "fastapi>=0.141.1",
    "sqlalchemy[asyncio]>=2.0.52",
    "uvicorn>=0.52.4",
]

[project.scripts]
api = "blunder.api:api"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/blunder"]

[tool.uv.sources]
blunder-shared = { workspace = true }

[tool.alembic]
script_location = "migrations"
prepend_sys_path = [ "." ]
